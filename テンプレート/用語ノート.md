# 用語ノート・テンプレート（コピー用）

> **このファイルはテンプレートです（`テンプレート/` に置く・集計しない）。**  
> 複製先: `用語/{{用語名}}.md`（ファイル名＝用語名。`用語一覧.md` に載っている語のみ）。先頭のこの説明は**保存前に削除**。  
> **構成**: 見出し1＝用語名 → 直後に `#用語` → 本文（定義・要点。他用語は `[[用語名]]`。末尾に「関連」一覧は付けない）→ **`## 出典` のみ**（「関連」セクションは作らない）。  
> **出典**: 過去問由来なら `[[問題/…]]` の wiki のみ（問題ノート見出しに ap-siken URL があるので出典に外部URLは不要）。過去問以外の根拠だけあるときのみ外部リンク（例: ap-siken 用語集）。二重に書かない。  
> **他用語のリンク**: 本文の語が一覧にあれば `[[用語名]]`。候補は `python3 scripts/check_question_terms.py --suggest --term 用語/…`（AI 手順は README）。

---

# {{用語名}}

#用語

{{定義・一文説明}}

- {{要点。一覧にある他用語は [[他用語名]] にリンク}}
- {{要点}}

## 出典

- [[問題/…]]
